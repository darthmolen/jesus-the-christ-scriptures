# Platform entry-point boilerplate is generated by the MAUI single-project
# template. Treat it as generated code so analyzers/StyleCop skip it, without
# weakening the global rules for first-party app code.
[*.cs]
generated_code = true
